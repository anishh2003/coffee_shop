import 'package:flutter/material.dart';

var kColorLightScheme = ColorScheme.fromSeed(
  seedColor: const Color(
    0xFFC88850,
  ),
);
var kColorDarkScheme = ColorScheme.fromSeed(seedColor: const Color(0xFF042A49));
